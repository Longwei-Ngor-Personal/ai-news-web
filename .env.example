# Copy this file to .env and adjust if needed.
# For now, the default SQLite path in config.py will work without changes.

# Example if you want to override DB location:
# DATABASE_URL=sqlite:///./data/news.db

ENVIRONMENT=production
APP_NAME=AI News Web Digestor API
